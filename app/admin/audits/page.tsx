export default async function Audits() {
  // placeholder – could list /api logs if desired
  return (
    <div>
      <h1 className="text-2xl font-bold">Audit Trail</h1>
      <p>All critical actions are recorded in the database (Audit table).</p>
    </div>
  );
}
